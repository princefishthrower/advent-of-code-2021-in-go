package main

import (
	"advent/utils"
	"fmt"
	"strings"
)

// string mapping
const (
	inp string = "inp"
	add        = "add"
	mul        = "mul"
	div        = "div"
	mod        = "mod"
	eql        = "eql"
)

func main() {
	lines := utils.ReadLines("input.txt")
	for _, line := range lines {

	}

}

func runCommand () {
	switch 
}